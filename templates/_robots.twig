{# ////////////////////////////////////////////////////////////////////////// #}
{# Robots                                                                     #}
{# ////////////////////////////////////////////////////////////////////////// #}

{# How to use: =================================================================

   Copy and paste the following Twig include:

   {% include "_robots" %}

   ...into your robots.txt template field found in the CMS here:

   SEOMatics > Global SEO > Robots

   If you don't have SEOMatic installed, you can delete this file entirely.

   NOTE: Any changes to the code below will not take effect until you save the
   settings on the SEOMatics > Global SEO > Robots page in the CMS

============================================================================= #}

# robots.txt for {{ siteUrl }}

Sitemap: {{ seomatic.helper.sitemapIndexForSiteId() }}

User-agent: seranking_audit_bot
User-agent: SEO-PowerSuite-bot
Allow: /

{% switch getenv('ENVIRONMENT') %}

{% case "production" %}

# Production - don't allow web crawlers to index cpresources/ or vendor/

User-agent: *
Disallow: /cpresources/
Disallow: /assets/resources/
Disallow: /vendor/
Disallow: /.env

{% case "staging" %}

# Staging - disallow all

User-agent: *
Disallow: /

{% case "dev" %}

# Dev - disallow all

User-agent: *
Disallow: /

{% default %}

# Default - don't allow web crawlers to index cpresources/ or vendor/

User-agent: *
Disallow: /cpresources/
Disallow: /assets/resources/
Disallow: /vendor/
Disallow: /.env

{% endswitch %}
