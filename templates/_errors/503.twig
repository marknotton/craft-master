{% extends "_layouts/error" %}

{% set page = 'holding-page' %}

{% block content %}
  <main>
    {{ svg('logo') }}

		{% if environment == "staging" %}

		  <h1>{{ "E008"|t|render_text }}</h1>
      <p>{{ "E009"|t }}</p>
      <a class="button" href="{{ siteUrl|replace('staging.', 'www.') }}">{{ "E010"|t }}{{ symbol('arrow-right') }}</a>

	  {% else %}

			<h1>{{ "E025"|t }}</h1>
      <p>{{ "E026"|t }}</p>

		{% endif %}
  </main>
{% endblock %}
