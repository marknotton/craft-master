{# ////////////////////////////////////////////////////////////////////////// #}
{# Header                                                                     #}
{# ////////////////////////////////////////////////////////////////////////// #}

<header>

  {% include '_components/image' %}

	<div>
		{{ title|h1 }}
		{{ entry is defined and not entry.isFieldEmpty('subtitle') ? entry.subtitle|h2 : false }}
	</div>

  {{ entry is defined and not entry.isFieldEmpty('introduction') ? entry.introduction|h2 : false }}

  {{ (header ?? false) and header|type != 'boolean' ? header|raw : false }}

</header>
