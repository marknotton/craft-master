<header>

  {% include '_components/featured-image' %}

  {{ title|h1 }}

  {{ entry is defined and not entry.isFieldEmpty('introduction') ? entry.introduction|h2 : false }}

  {{ (header ?? false) or header|type != 'boolean' ? header|raw : false }}

</header>
